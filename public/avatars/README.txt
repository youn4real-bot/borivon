Member avatar photos for the "Learn German Online" social-proof cluster
(components/OnlineCoursesRegistration.tsx → FACES array).

Drop 5 photos here, named EXACTLY:

  face1.jpg   ← the front-most one (shown on top of the stack)
  face2.jpg
  face3.jpg
  face4.jpg
  face5.jpg

Notes:
- Square-ish photos work best (they're cropped to a circle).
- .jpg / .jpeg / .png all fine — but the filename must end in .jpg here, or
  edit the FACES array in components/OnlineCoursesRegistration.tsx to match.
- Until a file exists, that slot shows a warm gradient circle (never a broken
  image), so the page always looks intact.
- Use only photos you have the right to use (real students/staff who consented,
  or licensed stock). Don't present strangers as enrolled members.

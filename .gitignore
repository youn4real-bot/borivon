# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# cloudflare / opennext build output
/.open-next/
/.wrangler/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

.vercel
.env*.local

# Claude Code session files
.claude/

# MediaPipe tasks-vision WASM for classroom virtual backgrounds (~19MB) —
# regenerated from node_modules by scripts/copy-mediapipe-assets.mjs on
# postinstall (incl. Vercel build). The small selfie_segmenter.tflite model
# beside it IS committed (not in any npm package).
/public/mediapipe/wasm/

# stray files
*.pdf

# Local deploy capture logs (vercel CLI stdout) — never committed.
# Pattern catches both `deploy_<slug>.log` and bare `deploy2.log` style.
deploy*.log

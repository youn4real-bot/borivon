// OpenNext → Cloudflare adapter config. Defaults are fine for the spike;
// caching/queues can be tuned later. Docs: https://opennext.js.org/cloudflare
import { defineCloudflareConfig } from "@opennextjs/cloudflare";

export default defineCloudflareConfig();
